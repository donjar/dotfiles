# vim: set noexpandtab:
[user]
	name = Herbert Ilhan Tanujaya
	email = mail@herbert.id
[diff]
	renames = true
[color]
	ui = true
[push]
	default = simple
[core]
	excludesfile = /home/donjar/.global_gitignore
[help]
	autocorrect = 1
[alias]
	aa = add -A
	ap = add -p
	bd = branch -D
	ca = commit --amend
	cb = checkout -b
	cl = clean -fdxi
	cm = commit -m
	co = checkout
	cp = checkout -p --
	dc = diff --cached
	d = diff
	dh = diff HEAD^
	dno = diff --name-only
	dw = diff --word-diff
	lg = log --oneline --graph
	ma = merge --abort
	pf = push --force-with-lease
	puo = push -u origin
	ra = rebase --abort
	rc = rebase --continue
	rem = rebase master
	rh = reset HEAD .
	rim = rebase -i master
	rpo = remote prune origin
	s = status
	stp = stash pop
	sts = stash show -p
	st = stash
